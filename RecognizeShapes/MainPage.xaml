<Page
    x:Class="RecognizeShapes.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RecognizeShapes"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:RecognizeShapes.Controls"
    xmlns:hlp="using:RecognizeShapes.Helpers"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <StackPanel Orientation="Horizontal">
            <InkToolbar TargetInkCanvas="{Binding ElementName=WritingArea}" x:Name="toolbar" />
            <controls:InkDevicesToolbar VerticalAlignment="Top" TargetInkCanvas="{Binding ElementName=WritingArea}" />
        </StackPanel>

        <Button Content="Clear" x:Name="ClearButton" Click="ClearButton_Click" Grid.Column="1" Padding="36 8" Margin="0 0 4 0" />

        <InkCanvas x:Name="WritingArea" Grid.Row="1" Grid.ColumnSpan="2"
                   hlp:AutoRecognizeShapes.Timeout="3000"
                   hlp:AutoRecognizeShapes.InkToolbar="{Binding ElementName=toolbar}"
                   hlp:AutoRecognizeShapes.TargetCanvas="{Binding ElementName=canvas}" />

        <Canvas x:Name="canvas" Grid.ColumnSpan="2">
            <Canvas.Resources>
                <SolidColorBrush x:Key="PolygonBrush" Color="Blue" />
                <SolidColorBrush x:Key="EllipseBrush" Color="Green" />
                <Style TargetType="Polygon">
                    <Setter Property="Stroke" Value="Blue"/>
                    <Setter Property="StrokeThickness" Value="2"/>
                </Style>
                <Style TargetType="Ellipse">
                    <Setter Property="Stroke" Value="Green"/>
                    <Setter Property="StrokeThickness" Value="2"/>
                </Style>
            </Canvas.Resources>
        </Canvas>

    </Grid>
</Page>